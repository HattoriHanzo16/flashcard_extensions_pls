// Server configuration
// Using HTTPS URL to prevent mixed content issues when on HTTPS sites
export const API_BASE_URL = 'http://localhost:3001';

// Other configuration options
// export const DEFAULT_USER_ID_PREFIX = 'user_'; 